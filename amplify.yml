version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd portfolio-site
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: portfolio-site/build
    files:
      - "**/*"
  cache:
    paths:
      - portfolio-site/node_modules/**/*
